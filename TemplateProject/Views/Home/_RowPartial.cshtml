@model Product

<tr>
    <td>@Model.Name</td>
    <td>@Model.Price.ToString("c")</td>
    <td>@Model.CategoryId</td>
    <td>@Model.SupplierId</td>

   @* Якорный элемент*@
    <td>
        <a asp-action="index" asp-controller="home" asp-route-id="@Model.ProductId" class="btn btn-sm btn-info">
            Select
        </a>
    </td>
</tr>